/*
sintaxe: <tipo> <nome> = <valor>;

tipos primitivos:
int (inteiro)
float (real) - precisão de 6 casas decimais
double (real duplo) - precisão de 10 casas decimais
char (caractere)
bool (booleano) - precisa conter a biblioteca stdbool.h
*/

#include <stdbool.h>

int main() {
    int idade;
    float salario = 1000.50;
    double porcentagem_desconto = 0.1;
    char genero = 'M';
    bool estaAprovado = false;
    return 0;
}
    
